export const validateRegister = (body) => { const errors = []; if (!body.name) errors.push('name is required'); if (!body.email) errors.push('email is required'); if (!body.password || body.password.length < 6) errors.push('password must be at least 6 characters'); return errors; };
export const validateLogin = (body) => { const errors = []; if (!body.email) errors.push('email is required'); if (!body.password) errors.push('password is required'); return errors; };
